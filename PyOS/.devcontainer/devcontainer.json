{
    "name": "PyOS",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "settings": {},
    "extensions": [
        "ms-vscode.cpptools",
        "ms-azuretools.vscode-docker"
    ],
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y nasm qemu"
}
